# Account details are intentionally blank. The PixelTouch team is no longer used;
# a brand-new Apple Developer account and Team ID are being created for Caroullage.
# Fill these in once App Store Connect setup is complete.
# Everything else gated on that account is tracked in docs/step-06-account-gated.md.

app_identifier("com.devron.caroullage") # matches project.yml; revisit if the new account uses a different prefix
apple_id("")                            # TODO: Apple ID of the new developer account
team_id("")                             # TODO: Developer Portal Team ID — the signing certificate's OU, not the ID in its CN
itc_team_id("")                         # TODO: App Store Connect team ID
